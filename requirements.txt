# Gate 0-2 dependencies only (no AI stack yet).
# FROZEN on 2026-09-22 from a verified Gate 0 run (Windows 11, Python 3.13.9, no NVIDIA GPU).
numpy==2.5.3
opencv-python-headless==5.0.0.93
pytest==9.1.1
